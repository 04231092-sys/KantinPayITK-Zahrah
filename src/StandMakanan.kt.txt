class StandMakanan(val nama: String) {
    private val daftarMenu = mutableListOf<Menu>()

    fun tambahMenu(menu: Menu) {
        daftarMenu.add(menu)
    }

    fun getMenu(nama: String): Menu? {
        return daftarMenu.find { it.nama == nama }
    }
}