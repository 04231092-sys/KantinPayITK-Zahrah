fun main() {

    val mhs = Mahasiswa("Zahrah", "1234", 10000.0)
    val menu = Menu("Ayam Geprek", 15000.0, 1)
    val sistem = SistemKantinPay()

    // ❌ Saldo kurang
    sistem.beliMenu(mhs, menu, "1234")

    // ✅ Top up
    mhs.topUp(10000.0)
    sistem.beliMenu(mhs, menu, "1234")

    // ❌ Stok habis
    sistem.beliMenu(mhs, menu, "1234")

    // ❌ PIN salah
    sistem.beliMenu(mhs, menu, "0000")
}